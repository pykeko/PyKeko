# PyKeko release history

One row per release. Tracks **dmg size** + the **headline feature** that landed.
For the full per-version changelog, see [`CLAUDE.md`](CLAUDE.md) or the
[GitHub Releases page](https://github.com/pykeko/Moorhen-PyKeko/releases).

To refresh sizes from the GitHub API (or after publishing a new release),
run [`tools/release-sizes.sh`](tools/release-sizes.sh) and paste its output here.

## Sizes & headline features

| Version | Date | dmg size | Δ prior | Δ vs v0.1 | Headline |
|---|---|---:|---:|---:|---|
| pk-v0.1   | 2026-05-25 | **176.48 MB** | — | — | First macOS Tahoe build — branded fork shipped end-to-end |
| pk-v0.2   | 2026-05-28 |   188.23 MB | +11.75 | +11.75 | CLI integration (`pykeko file.pdb`, fetch by PDB id, `.pml` scripts) + residue torsion editor |
| pk-v0.2.3 | 2026-06-02 |   226.63 MB | **+38.40** | +50.15 | MVS portable-viewer export (Mol\* runtime inlined into the bundled viewer template — this is where the size jumped) |
| pk-v0.2.4 | 2026-06-02 |   226.63 MB |  +0.00 | +50.15 | Camera-follow density in portable viewer (rolling-cube clip) + export skip-density dialog |
| pk-v0.2.5 | 2026-06-02 |   226.64 MB |  +0.01 | +50.16 | Shell-style history in Interactive Scripting modal (↑/↓ recall, Cmd+Enter submit) |
| pk-v0.2.6 | 2026-06-02 |   226.64 MB |  +0.00 | +50.16 | Regression fixes (density camera-follow restored, screenshot upscale restored, ligand CID parser fix) |
| pk-v0.2.7 | 2026-06-03 |   226.63 MB | −0.01 | +50.15 | Persistence fixes — static-server port pinned (welcome modal / scripting history actually persist). **SUPERSEDED: shipped with preload regression that broke `__moorhenControl`** |
| pk-v0.2.9 | 2026-06-03 |   226.63 MB |  +0.00 | +50.15 | Preload regression fix + upstream Moorhen bug-fix catchup (controls-lock release, modal default position, padded vector names, mmdb→gemmi output). **First build since v0.2.6 where every Electron-only menu item works** — including the session save/restore introduced in (the un-shipped) v0.2.8 |
| pk-v0.2.10 | 2026-06-03 |   226.63 MB |  +0.00 | +50.15 | `File → Open files…` rebuilt as a real clickable `MoorhenMenuItem` (previously an inert "Open Files" header next to an invisibly-styled Browse… button); `pykeko -h` / `--help` print a one-screen usage summary (launcher script intercepts `--help` because Chromium eats it before main.js sees it) |
| pk-v0.2.11 | 2026-06-04 |   226.63 MB |  +0.00 | +50.15 | PyMOL `color` command: dropped over-eager `cidsOverlap` gate so the inverse-push reaches reps whose CIDs cidChains regex couldnt read (full 4-seg CIDs, residue ranges, compound `||`-joined cids); added a one-line diagnostic in pykeko.log per color invocation |
| pk-v0.2.12 | 2026-06-04 |   226.63 MB |  +0.00 | +50.15 | `Ligand → New Ligand from SMILE…` gains a "Place at" dropdown (View centre / Nearest positive Fo-Fc peak) + an "Auto-fit to active map (jiggle + RSR)" toggle, both default-off. Together they reproduce Coot 0.9.x's "Fit ligand here" workflow as a single dialog. |
| pk-v0.2.13 | 2026-06-04 |   227.65 MB |  **+1.02** | +51.17 | Portable viewer bumped to Mol\* 5.9.0 (was 4.18). dsehnal confirmed declarative MVS clip on volume representations shipped in Mol\* 5; clean dep-only bump with no source changes. Inlined viewer HTML grew ~45 KB gzip. Camera-follow density clip in App.tsx still imperative (Mol\* did not add declarative camera-follow). |
| pk-v0.2.14 | 2026-06-04 |   227.65 MB |  +0.00 | +51.17 | **Hotfix.** v0.2.12 introduced a double-negation in the SMILES→ligand placement code: `placement` was set in world coords then negated again before passing to Coot, so the ligand landed at the mirror across (0,0,0) — miles from any density. Peak path silently fell back to view-centre on no-diff-map → both options produced the same wrong spot, and auto-fit's jiggle radius was way smaller than the protein-to-mirror distance. Fix: pass world coords directly. |
| pk-v0.2.15 | 2026-06-04 |   227.65 MB |  +0.00 | +51.17 | **SMILES placement, take 2.** v0.2.14 fixed double-neg but "View centre" = camera rotation centre isn't on the protein on fresh loads. New default "Active molecule centre" uses the protein's atom centroid. Caught a second mirror-bug while testing: `centreOnGemmiAtoms` returns the **negated** centroid (its name lies; it's meant for `setOrigin`). Also: "two ligands stacked" on merge-into-molecule (standalone was hidden but not deleted) — standalone now fully disposed. View auto-recentres on the placement target. |
| pk-v0.2.16 | 2026-06-05 |   228.65 MB |  +1.00 | +52.17 | Per dsehnal's comments on molstar/molstar#1844, declarative camera-follow clip won't happen at the MVS layer — the throttled-mutation pattern `wireCameraFollowDensity` uses is the maintainer-endorsed approach. The STATIC initial clip CAN move to MVS though (`representation.clip({type: sphere, center, radius, invert, variant: pixel})` shipped in Mol* 5.0). MvsExportBuilder now emits it. Recipient .html viewer-template behaviour unchanged (seed call still re-anchors to recipient camera), but the file is more self-describing for non-PyKeko MVS viewers. `App.tsx` header rewritten to cite the dsehnal architecture endorsement so the next reader doesn't question the design. |
| pk-v0.2.17 | 2026-06-05 |   228.65 MB |  +0.00 | +52.17 | MVS portable-viewer export: PyMOL `lines` now exports as visibly-thinner geometry rather than identical-thickness `sticks`. The in-app translator distinguishes them only via `bondOptions.width` (0.03 vs 0.10); the exporter now threads that through and emits MVS `ball_and_stick.size_factor` = width / 0.10. |
| pk-v0.2.18 | 2026-06-05 |   151.00 MB | **−75.00** (−33%) | **−24.00** | **Coot WASM patch + dmg slim-down.** PyMOL color commands had no visible effect on bond/stick reps; root cause was a Coot WASM CID-selector bug (set_user_defined_atom_colour_by_selection ignored its CID argument and always coloured the same chain) AND upstream Moorhen never called set_use_bespoke_carbon_atom_colour. Patch coot-patches/coot-molecule-bonds-userdef-color-cid-fix.patch bypasses mmdb's broken Select for whole-chain CIDs, walks model manually. JS-side: bespoke toggle in getCootSelectionBondBuffers, dedupe-then-append in cmdColor. Separate: forge.config.js gained an ignore pattern excluding viewer-template/node_modules, .attic, out — was bundling them into Resources/app/ before. **Net dmg size: 226.63 → 151.00 MB**, a 33% reduction. |
| pk-v0.2.34 | 2026-06-14 |   150.98 MB |   0.00 | −25.50 | **RSR-aware covalent bonds (Path A1 — refmac extra restraints).** Until v0.2.33, declared covalent bonds were live-display-only: the augmented mmCIF carried `_struct_conn` for external refmacat, the mod2 dict updated the bond orders in the viewer, but in-app real-space refinement had no idea the bond existed — RSR would treat Cβ and Cys-S as unconnected atoms and let them drift. v0.2.34 closes the loop by loading a one-line refmac `EXTE DIST` record into `molecule_t::extra_restraints` (`coot::molecules_container_t::read_extra_restraints` via a new `shim_load_extra_restraints_string` helper that writes to the WASM virtual FS, calls the parser, unlinks). Once loaded, `refine_direct()` auto-applies them (verified at `coot-1.0/api/coot-molecule.cc:2654`), so the user-facing `refine_residues_using_atom_cid` path now honors the bond — atoms stay paired through refinement, no external refmac round-trip needed for in-app RSR. Refmac record built per family: bond length 1.81 Å (F1/F3/F5), 1.78 Å (F2), 1.83 Å (F6); σ=0.02 Å. Why this and not the obvious `make_covalent_link` binding: it's the same embind silent-drop trap that bit Edit Phi/Psi (see [[moorhen-embind-silent-drop]]); `read_extra_restraints` pre-dates the trap and works. `declareCovalentLink` now returns `rsrAwareUpdated: true` only when the parser actually ingests the record (parsedCount > 0) — false signal would otherwise be hidden by the existing `liveDisplayUpdated`. Limitation: pure-geometric `minimize_energy` does NOT call `add_extra_restraints` (only `refine_direct` does), so the geometry-only menu path can't pull the bond — documented in the executor's path note. End-to-end smoke: declare CYS-481-SG → 1E8-701-CAA on 5P9I, parser returns 1, `rsrAwareUpdated=true`. |
| pk-v0.2.33 | 2026-06-14 |   150.98 MB |   0.00 | −25.50 | **MCP wire-up + F1/F3/F4 validation pass.** No new chemistry but consolidates the covalent workflow with empirical cross-checks. (1) **New `declareCovalentLink` control verb in `MoorhenControlApi`** + matching `moorhen_declare_covalent_link` MCP tool (PyKekoMCP bumped 0.1.0 → 0.2.0). Lets Claude or any MCP-aware agent declare covalent links in chat: loads the link CIF, writes augmented mmCIF with `_struct_conn`, downloads it, applies mod2 for live bond-order update. Routes through the v0.2.29+ `executeCovalentLink` helper. Verb appears in API alongside existing `loadFiles`/`refine`/etc. (2) **F3 AceDRG cross-validation** (`docs/covalent-ligand-survey-2026-06-14/acedrg-cys-caa/COMPARISON.md`): AceDRG's CYS-CAA reference matches my hand-authored `CYS-CAA.cif` targets to 0.002 Å / 0.2° / 0°. F3 validates clean. (3) **F4 AceDRG cross-validation** (`acedrg-cys-epx/COMPARISON.md`): AceDRG can't perform F4's ring-opening (LINK directive can't encode bond-deletion + add-H-on-O), produces ring-still-closed geometry instead. My post-product targets (sp³ thioether, 100°/110°) are correct; AceDRG's 109.5° reflects ring-strained geometry. (4) **F1 empirical PDB measurement** (`pdb-cys-acr/COMPARISON.md`): measured 5P9J (8E8 saturated ibrutinib + BTK). d(SG-Cβ) = 1.85 vs my 1.81 target (within 2σ). Angles wider in deposit (113° vs my 100°) likely from refinement without chem_link — F3 AceDRG values (101°) agree with mine, so deposit values aren't canonical. CYS-ACR.cif angle targets are correct. Architecture takeaway captured in release notes: AceDRG validates LINK GEOMETRY but not CHEMISTRY TRANSFORMS (bond-order changes) for F1/F2/F5/F6 — that's the value-add of PyKeko's mod2 system. |
| pk-v0.2.32 | 2026-06-14 |   150.98 MB |   0.00 | −25.50 | **F4 epoxide + F6 reversible carbonyl warhead families.** Completes the F1-F6 plan-doc taxonomy — registry now has 14 entries across all 6 Cys-electrophile chemistries (F1×3, F2×3, F3×2, F4×2, F5×2, F6×2). (1) **F4 epoxide**: Cys-S attacks 3-ring C; ring opens; O migrates to the other ring C as -OH. Covers E-64 family (Cys protease inhibitors), epoxysuccinyl probes, terminal-epoxide tool compounds. Two entries: `CYS-EPX-post` (β-hydroxy thioether, OH on Cα distinguishes from F1-post chain to next carbonyl), `CYS-EPX-pre` (closed 3-ring; mod2 deletes Cβ-O bond + adds H/O-H bond for the new hydroxyl, no carbon retype since ring strain release doesn't change hybridisation). Detection: Cβ has an O neighbour that is itself bonded to another C that is ALSO a direct neighbour of Cβ — unique 3-ring closure. (2) **F6 reversible carbonyl**: Cys-S adds to a non-amide C=O forming tetrahedral hemithioketal. Covers GAPDH active-site probes, reactive-aldehyde inhibitors. Reversibility is a binding-energetics property, not a structural one — for refmacat we treat bound state as a snapshot. Two entries: `CYS-RVC-post` (hemithioketal, Cβ sp³ with S/OH/R/R'), `CYS-RVC-pre` (free carbonyl; mod2 changes C=O→C-O single + adds H on O + retypes Cβ sp²→sp³). Geometry: S-Cβ 1.83 Å (longer than other sp³ thioethers; geminal -OH weakens S-C), SG-Cβ-O 109°. Detection: Cβ has =O and no N neighbour (distinguishes from amide carbonyls in F1/F2/F3/F5). Caveat: F6-pre SMARTS `[C:1]=O` is broad — picks up any ketone/aldehyde; user should only choose CYS-RVC-pre when intending covalent attachment. Architecture: `co`, `n`, `o` fields on `CovLinkAtomMap` are now optional; new fields `cl` (F3), `oe`/`hoe` (F4), `ho` (F6); `applyAtomMap` gates every non-required substitution; new `buildAtomMapF4`/`buildAtomMapF6` helpers. `MoorhenCovalentLinkOrchestrator.ts` deliberately kept — has active unit test coverage. |
| pk-v0.2.31 | 2026-06-14 |   150.98 MB |  +0.01 | −25.50 | **F3 chloroacetamide + F5 maleimide warhead families.** Brings the registry to 5 chemistries × 10 entries. (1) **F3 α-chloroacetamide**: SN2 displacement of Cl by Cys-S (`R-N-C(=O)-CH₂-Cl + Cys-S⁻ → R-N-C(=O)-CH₂-S-Cys + Cl⁻`). Covers GSK probes, PROTAC linkers. Two entries: `CYS-CAA-post` (saturated form, Cβ directly bonded to carbonyl — distinguishable from F1-post by the single-carbon warhead structure), `CYS-CAA-pre` (chloride form, mod2 deletes Cl + the Cβ-Cl bond, no Michael H added since SN2 swaps Cl→S without protonation). Geometry: S-Cβ 1.81 Å sp³ thioether, SG-Cβ-CO 110° tetrahedral compressed by adjacent carbonyl. New detector branch + `buildAtomMapF3` for the single-carbon warhead structure. (2) **F5 maleimide**: Michael addition across the maleimide ring C=C → 3-thiosuccinimide (ring stays closed, becomes saturated). Covers ADC linkers, fluorescent probes (Alexa Fluor maleimides, fluorescein-5-maleimide), bioconjugation tags. Two entries: `CYS-MAL-post` (saturated 3-thiosuccinimide, SMARTS distinguishes from F1-post by ring closure — Cβ has a second carbonyl-C besides the chain Cα-CO, and the two ring carbonyls share an amide-N), `CYS-MAL-pre` (maleimide ring form, mod2 transforms ring C=C → C-C single + adds H on Cα + retypes both sp²→sp³; Cβ keeps its single H unlike F1-pre-terminal). Geometry: same 1.81 Å sp³ thioether as F1/F3, SG-Cβ-Cring 112° (slight ring-strain compression vs acyclic 113°), no planarity restraint. Detector branch reuses the existing F1/F2 loop with a maleimide ring-closure check: Cβ has both a `=Cα` neighbour AND a separate single-bond carbonyl-C, and that second carbonyl's amide-N is shared with Cα's carbonyl's amide-N. Also: `bondLengthForFamily` and the SMILES dialog placement seed now route F3/F5 through the same sp³ placement code as F1; `applyAtomMap` gates `<CA>` and `<CG>` substitution since F3 doesn't use them. |
| pk-v0.2.30 | 2026-06-14 |   150.97 MB |  +0.01 | −25.51 | **Covalent workflow polish.** No new chemistry, but every piece of the v0.2.29 covalent pipeline now works the way it should. (1) **SMILES-time placement geometry seed.** When "Form covalent bond to Cys SG" is enabled in the SMILES dialog, the new ligand lands with its Cβ atom on the (Cys-CB → SG) axis extended by the canonical S-Cβ bond length (1.78 Å F2, 1.81 Å F1). Implementation pre-offsets the centroid placement by the dict's internal (centroid - Cβ) so one `get_monomer_and_position_at` call puts Cβ exactly on target — no follow-up translate. Lives in `MoorhenCovalentPlacement.ts`. Means you find the Cβ atom right next to the picked Cys instead of hunting across the protein. (2) **Right-click panel auto-detects the warhead.** When you pick/type a Cβ CID, the panel runs the detector against the ligand's cached chem_comp dict, auto-selects the matching registry entry in the "Link template" dropdown, and de-emphasises entries from the other family. Header shows `(auto-detected: CYS-XXX-…)` so you can see what was picked. Manual override still works. (3) **HCA name collision fix in `buildAtomMap`.** Pre-v0.2.30 the atom-name map left `hca` undefined, so mod2 substitution left `<HCA>` literal in the CIF text and the Michael-addition proton was never added to the dict for F1 alkene / F2 alkyne pre-reaction inputs (only bond order updated). Now derive an `H<n>` name matching ligand H-numbering convention with fallthrough to `H<n>A/B/C…` and `H<n>_0/1/…` until guaranteed-unique. (4) AMIDE_PLANE doc cleanup — confirmed both F1 and F2 link CIFs have no `<AMIDE_PLANE>` references via grep on 2026-06-14. |
| pk-v0.2.29 | 2026-06-11 |   150.96 MB |  −0.02 | −25.52 | **F1 acrylamide family + SMILES-time covalent attachment + live-display bond-order update.** Three coupled additions to the covalent-ligand workflow. (1) **F1 acrylamide warhead family** — ibrutinib, osimertinib, zanubrutinib (acryl form), and the rest of the Cys-S-α,β-unsat-amide drug class are now first-class. Authored `CYS-ACR.cif` (S–Cβ 1.81 Å single, SG–Cβ–Cα 113°, no plane — sp3 thioether geometry) and `CYS-ACR-mod2-alkene.cif` (alkene C=C → saturated C-C, add H on Cα, retype sp²→sp³). Three registry entries: `CYS-ACR-post`, `CYS-ACR-pre`, `CYS-ACR-pre-terminal`. Detector extended to handle F1 from the bond graph + accepts a `preferFamily` hint for the F2-post-vinyl vs F1-pre-alkene bond-order-2 ambiguity. (2) **SMILES-time covalent attachment** — `Ligand → New Ligand from SMILES…` dialog gets an opt-in "Covalent attachment" section with a Cys SG picker (using `usePauseClickAwayListener`) and a warhead-template dropdown. When enabled, the ligand is generated + merged as today; a snackbar then prompts the user to left-click any Cβ atom on the merged ligand, and that click triggers the full link declaration (load link CIF, write `_struct_conn`, download augmented mmCIF). No more "load → merge → right-click → declare → re-open → duplicate" dance. (3) **Live-display bond-order update** — after the link declares, the executor runs the warhead detector on the loaded ligand's chem_comp dict, builds the atom-name map, applies the mod2 transforms (delete H, change bond order, retype atoms) directly to the dict CIF text, and reloads via `read_dictionary_string` + `setAtomsDirty` + redraw. Coot's `init_refmac_mon_lib` cleanly overwrites the existing entry when the read_number bumps; per-molecule `imol_enc=molNo` scoping keeps the override local. F2 alkyne → vinyl thioether (C≡C → C=C + H on Cα + sp→sp²) and F1 alkene → saturated thioether (C=C → C-C + H on Cα + sp²→sp³) both update in the viewer — no refmacat round-trip needed for the display. Augmented mmCIF still downloads for refmacat; refinement behaviour unchanged. **Refactor**: both right-click and SMILES-time flows route through one `executeCovalentLink` helper; ~80 lines of duplicated surgery removed from `MoorhenCovalentLinkButton.tsx`. |
| pk-v0.2.28 | 2026-06-11 |   150.98 MB |  +0.00 | −25.50 | **PyMOL color recolor + covalent picking dismissal.** (1) `color red, chain A` left existing sticks/lines grey. The v0.2.18 first-match-wins dedupe walked only molecule.defaultColourRules — per-rep `colourRules` arrays accumulated stale earlier same-cid rules, and Coot's `shim_set_bond_colours` ships the whole list and picks first-match-wins per atom, so the older grey rule kept winning for CB-family reps (cartoons were fine via Coot's per-rule last-wins path). Extended the dedupe to per-rep arrays gated by a reference check (skip the shared-defaults case already handled above) and explicitly skipping multi-colour rules so rainbows/b-factor/electrostatics aren't stripped. (2) Covalent panel "Pick in viewer" → left-click an atom dismissed the panel. Canvas mousedown synchronously dispatched the atomClicked event but the browser synthesized a follow-up click which MoorhenClickAwayListener (listens on `click`, not `mousedown`) treated as outside the popover's React subtree → setIsShown(false) → panel unmounts before the picked CID is seen. Fixed via the existing usePauseClickAwayListener hook (MoorhenCidInputForm uses this exact pattern). Status copy now reads "Left-click any atom in the 3D viewer…" so the mouse button is unambiguous. |
| pk-v0.2.27 | 2026-06-08 |   150.98 MB |  +0.01 | −25.50 | **Covalent-link panel: fixed top-right slot + obvious drag bar.** UX fix for the dialog. v0.2.24 made it draggable in principle (h6 title was the handle) but the affordance wasn't obvious — and even with dragging, the panel still spawned on top of the atoms the user was trying to pick. Repositioned via `position: fixed; top: 80; right: 16; zIndex: 2000` — same viewport corner every time regardless of right-click site. Replaced the h6 title with a visible grey title-bar-style strip carrying `⋮⋮` grab dots, the panel name, and `cursor: move`. Stays inside the popover's DOM hierarchy (no React Portal), so the ClickAwayListener correctly treats clicks on the panel as in-bounds. |
| pk-v0.2.26 | 2026-06-08 |   150.97 MB |  +0.00 | −25.51 | **mmCIF tokenizer fix: covalent-link "atom not found" cascade.** v0.2.25 fired `Atom //A/547/SG not found in model atom_site loop` for every covalent-link declaration. Root cause: Coot's `molecule_to_mmCIF_string_with_gemmi` writes `label_atom_id` with PDB-style column padding inside single quotes (`' SG '` not `SG`), and `tokenizeMmcifRow` extracted the quoted substring verbatim — preserving the spaces. Every `cols[idxAtom] === "SG"` was false. Same tokenizer was used by every v0.2.20 JS-side embind workaround (`getTorsionJs`, `addWaterAtPositionJs`, `setPhiPsiJs`, `getNcsGhostMatrixJs`) — all four were silently emitting "atom not found" responses but the failure was invisible from the user side. One-line fix: trim quoted values in the tokenizer. Unquoted tokens can't contain whitespace by mmCIF rules so trim is a no-op there. Verified `findAtomInModel` now correctly resolves A:78 SG, A:135 SG, A:78 CA, and properly returns null for A:999 SG (missing). |
| pk-v0.2.25 | 2026-06-08 |   150.97 MB |  +0.01 | −25.51 | **PyMOL `;` separator + main-process SW cleanup that actually works.** Two bugs on top of v0.2.24. (1) **PyMOL script parser was line-only**: `hide spheres; color green, elem c` parsed as ONE `hide` command with args `['spheres; color green', 'elem c']`, emitted `hide: unsupported "spheres; color green"`, and the second command was never seen. Added top-level `;`-splitting (quote-respecting, paren-respecting) in MoorhenPymolParser so commands separated by either `;` or `\n` parse correctly. (2) **v0.2.24's SW cleanup was running too late**. The renderer-side `navigator.serviceWorker.getRegistrations().then(unregister)` in `src/index.tsx` lived INSIDE the new bundle — which the OLD service worker intercepted and replaced with the cached old bundle before the cleanup code could execute. So users upgrading from any pre-v0.2.24 install still saw the old bundle's behavior on first launch (user reported the v0.2.21-era covalent-link "Registry load failed: Unexpected token '<'" doubled-URL error on v0.2.24 — that was the v0.2.21 bundle being served, not the v0.2.24 code). Moved the cleanup to PyKeko `main.js`: `mainWindow.webContents.session.clearStorageData({storages:['serviceworkers']})` runs BEFORE `loadURL`, through Electron's session API rather than the SW. Version-agnostic — works regardless of what bundle the old install shipped. Verified end-to-end: `navigator.serviceWorker.getRegistrations()` returns `[]` on first launch after upgrade. |
| pk-v0.2.24 | 2026-06-08 |   150.96 MB |  +0.01 | −25.52 | **PWA disabled + CLI session load actually works + covalent dialog draggable + repeated-proto-field session fix.** Four bugs caught by user-reports against v0.2.22 ("can't pick second atom anymore", "menu blocks the atoms", "CLI session-load drops the file") and by my smoke-test of v0.2.23. (1) **vite-plugin-pwa disabled.** The autoUpdate workbox SW persisted across reinstalls and served the prior version's cached bundle on first launch after every upgrade — so the v0.2.22 covalent-pick fix shipped in `index-BrH9ZmZc.js` was hidden behind the v0.2.21 bundle the user's SW still served. v0.2.24 removes VitePWA from the build and adds a runtime unregister-all-SW + clear-workbox-caches step in `src/index.tsx` so users transitioning from any earlier release get cleaned up automatically. (2) **CLI `.pykeko` / `.pb` argv.** `LOADABLE_RE` in `main.js` filtered to `.pdb|.ent|.cif|.mmcif|.mtz|.mrc|.map|.ccp4|.gz` — session extensions never reached `loadFilesIntoRenderer`. Drag-drop worked (different code path); the CLI did not. (3) **Covalent-link panel draggable.** The overlay rendered wherever the right-click happened, which often sat directly over the ligand atoms the user was trying to pick. Wrapped in `react-draggable` with the h6 header as the drag handle. (4) **Session restore .map-of-undefined.** `Session.toObject(msg)` without `arrays:true` decodes empty `repeated` proto fields as `undefined`; `storedMoleculeData.defaultColourRules.map(...)` then throws. Drag-drop only escaped this when the session happened to have populated arrays — CLI session-load via `MoorhenControlApi.loadFiles` hit it deterministically. Now `arrays:true, objects:true` so every consumer sees a complete object. Also added `commandCentre.current.history.reset()` before the restore call to match the native `handleSessionUpload` path. **Note**: the pk-v0.2.23 tag exists on both repos but no GitHub release was ever cut at that sha — the smoke-test caught the .map bug in the v0.2.23 build before publish. |
| pk-v0.2.22 | 2026-06-08 |   150.95 MB |  +0.00 | −25.53 | **Hotfix for v0.2.21.** Four user-facing bugs caught on a fresh v0.2.21 install: (1) covalent-link panel's registry fetch URL doubled the assets prefix (urlPrefix already defaults to "MoorhenAssets") and got the SPA index.html fallback, JSON.parse choked on `<!doctype` — fixed; (2) link-template dropdown was empty for the same root cause — resolved; (3) the panel's "Pick in viewer" button never updated the ligand CID because the regex parsing the picked atom label expected `/<molNo>/` (integer) but the actual format is `/<mol_name>/` (file name like "8FD9") — now uses cidToSpec like every other atom picker; (4) `pykeko <session>.pykeko` from the CLI silently dropped the file. MoorhenControlApi.loadFiles classified files by extension and `.pykeko` / `.pb` fell through the unknown-extension branch — never reached MoorhenTimeCapsule.loadSessionFromProtoMessage. loadFiles now routes session files to the same restore code path as the native Open Session dialog, threaded via timeCapsule in MoorhenControlBridge's ctx. Drag-drop, File → Open Session…, and CLI argv now all share one entry point. **No content change vs v0.2.21 — pure bug fixes.** |
| pk-v0.2.21 | 2026-06-08 |   150.95 MB | −0.01 | −25.53 | **PWA cache fix + ChEMBL search + AceDRG fallback for SMILES→CIF.** vite-plugin-pwa filtered out of the dist SPA build — the Service Worker that's been caching the previous bundle in IndexedDB since v0.2.6 (and serving it on first launch after upgrade — feedback_pykeko_sw_stale_cache in auto-memory) is gone. v0.2.21 → v0.2.22 will be the first upgrade transition without the stale-cache hiccup; benefit compounds with every future release. SMILES menu gains ChEMBL as a 3rd source (alongside PubChem and raw SMILES paste); ChEMBL's API focuses on drug-like compounds and tends to return cleaner SMILES for approved drugs / known inhibitors. Both lookup paths now check response.ok and warn the failure mode in console instead of silently passing empty text downstream. Local AceDRG fallback for the SMILES→CIF path: when smiles_to_pdb (RDKit-WASM) returns nothing, the renderer shells out to a local acedrg via the new pykeko:acedrg-smiles IPC handler; honours AceDRG_BIN env var, then common CCP4 install paths (/Applications/ccp4-9/bin/acedrg etc.), then PATH. Graceful "install CCP4" message if not found. Browser builds skip the fallback (no IPC bridge). |
| pk-v0.2.20 | 2026-06-08 |   150.96 MB | −0.04 | −25.52 | **JS-side workarounds for an upstream embind silent-drop bug.** Any newly-added `.function()` line in Moorhen's `wasm_src/moorhen-*.cc` silently fails to register at runtime — binding name strings + code refs are in the WASM, but the registrations don't fire at init. Bit four shipped PyKeko features (Edit Phi/Psi, χ display, single-water `w`, NCS ghost matrix) — they'd been silent no-ops, with the UI looking like it worked but nothing happening (JS catch blocks swallowed the TypeError). All four now route through pure-JS implementations using working bindings (molecule_to_mmCIF_string_with_gemmi + replace_molecule_by_model_from_string) under the hood. Same release also ships the one-click Cys-covalent ligand declarator (task #148): right-click → ⛓ → click ligand atom → augmented mmCIF download. Reproduces in pristine moorhen-coot/Moorhen against emscripten 5.0.3 AND 5.0.7; `-DEMSCRIPTEN` doesn't fix it. Min repro + refuted-hypothesis table in Moorhen-PyKeko/docs/embind-silent-drop-bug.md. **v0.2.19 was skipped — closed without code (Moorhen's `Validation → Water validation…` already implements find_water_baddies).** |

> **v0.2.8** was tagged briefly during the session save/restore work but never properly released — the preload regression from v0.2.7 was still present and the session-save menu items fell through to the legacy browser path. v0.2.9 is the first build where that feature actually works. See [`feedback_electron_preload_require_trap.md`](https://github.com/hilgersmt/notes-or-wherever) for the diagnosis (or this repo's `CLAUDE.md`).

## Growth read

- **+50 MB / +28% over 9 days** total (v0.1 → v0.2.9)
- One big jump (+38 MB at v0.2.3 — bundling the Mol\* viewer runtime); everything else essentially flat
- The TypeScript/React side of the bundle (`static/assets/index-*.js`, ~3.8 MB) absorbs new features almost for free — incremental cost per release has been ~10 KB
- Future jumps to watch: a coot-wasm rebuild that pulls newer upstream `libcootapi` could move the needle (the `MoorhenAssets/wasm/` blobs are ~30 MB), and disabling vite-plugin-pwa in v0.3.0 will shed ~1 MB of `sw.js` + workbox precache index

## What's inside (rough breakdown of pk-v0.2.9's 226.63 MB)

- **Electron framework** — ~70 MB compressed
- **WASM blobs** (`moorhen64.wasm` + `moorhen.wasm` + `gemmi*.wasm`) — ~50 MB compressed
- **Mol\* viewer template** (`Resources/dist/index.html` inlined runtime) — ~22 MB compressed
- **MathJax precache** — ~5 MB
- **Monomer library + RDKit data** (packed inside `data_tmp/data.tar`) — ~10 MB
- **App JS bundle** (`static/assets/index-*.js`) — ~1 MB compressed (the entire React UI + PyMOL translator + control bridge + everything else)
- **Misc** (icons, fonts, SW, manifest, etc.) — remainder
